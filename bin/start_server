import os
import sys

if __name__ == "__main__":
	if len(sys.argv) == 2 and sys.argv[1] == '-p' :
		os.system("go run http_server.go")
		quit()
	if len(sys.argv) == 2 and sys.argv[1] == '-b' :
		os.system("go run http_backup.go")
		quit()

	print "Please specify the argument: -p or -b?"


import json
fin = open('conf/settings.conf')
conf = json.loads(fin.read())

PORT = int(conf['port'])
PRIMARY = conf['primary']
BACKUP = conf['backup']
ARB_STR = 'arb'
TIMEOUT = 300

TRUE = 'true'
FALSE = 'false'